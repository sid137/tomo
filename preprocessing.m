%reference_pulse = load pulse-reference.txt;
%raw_quadratures = load quadrature-pulses.txt;
%generatlize_quadratures = dot(reference_pulse, raw_quadratures);



for i = 1:20
   i 
end

%load data.txt

%quadrature = data(:,1);
%phase = data(:,2);
%shot = data(:,3);


%density_matrix = tomography(quadrature, phase, shot);

